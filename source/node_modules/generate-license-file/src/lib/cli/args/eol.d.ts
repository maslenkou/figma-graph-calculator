import { LineEnding } from "../../lineEndings";
import { CombinedConfig } from "../commands/main";
import { Argument } from "./argument";
export declare class Eol extends Argument<LineEnding | undefined> {
    private readonly choices;
    resolve(config: CombinedConfig): Promise<LineEnding | undefined>;
    parse(config: CombinedConfig): Promise<LineEnding | undefined>;
}
