import { CombinedConfig } from "../commands/main";
import { Argument } from "./argument";
export declare class Inputs extends Argument<string[]> {
    private question;
    resolve(config: CombinedConfig): Promise<string[]>;
    parse(config: CombinedConfig): Promise<string[]>;
    private resolveOne;
    private resolveMany;
    private getInputPromptInitialValue;
    private promptForTermination;
}
