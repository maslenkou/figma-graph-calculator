import { Dependency, LicenseContent } from "../resolveLicenses";
type ResolveLicensesOptions = {
    replace?: Record<string, string>;
    exclude?: string[];
};
export declare const resolveDependencies: (packageJson: string, licensesMap: Map<LicenseContent, Dependency[]>, options?: ResolveLicensesOptions) => Promise<void>;
export {};
