import { Dependency, LicenseContent } from "../resolveLicenses";
type ResolveLicensesOptions = {
    replace?: Record<string, string>;
    exclude?: string[];
    omitVersion?: boolean;
};
export declare const resolveDependenciesForPnpmProject: (packageJson: string, licensesMap: Map<LicenseContent, Dependency[]>, options?: ResolveLicensesOptions) => Promise<void>;
export {};
